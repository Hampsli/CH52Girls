/* TASK 1
Create and export a method to **return** the **URL** of the server from the SD-12-1: AssessmentServer-Individual project.

* The `getServerURL()` method should **return** the URL of your running JSON server. */

// Task 1: getServerURL()

export function getServerURL() {
    return "http://localhost:3000/"; //El URL lo obtuve corriendo el server en la carpeta de servergetServerURL
  }
  
